# Level 3 candidate sources

Candidate source files referenced by the V2 registry. They were extracted from local solver artifacts so a clean checkout can run candidate and compilation tests without private `recon-output` state. Verification status is recorded in `src/level3/candidates.ts`; raw server proof payloads are intentionally excluded.
