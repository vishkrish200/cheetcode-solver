# Level 3 candidate sources

Candidate source files referenced by the V3 registry. They are committed so the solver and tests do not depend on private `recon-output` state. Verification status is recorded in `src/level3/candidates.ts`; raw server proof payloads are intentionally excluded.
