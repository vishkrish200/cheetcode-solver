# Required only for an authorized live run.
CHEETCODE_GITHUB=your-github-handle
CHEETCODE_URL=https://ctf.firecrawl.dev

# Point to local, ignored browser artifacts when the current contract requires them.
# AUTH_STORAGE_STATE_PATH=recon-output/storage-state.json
# CHEETCODE_FINGERPRINT_HINTS_PATH=recon-output/fingerprint-hints.json

# Optional OpenAI-compatible fallback.
# CEREBRAS_API_KEY=
# LLM_MODEL=
# SMART_LLM_MODEL=
